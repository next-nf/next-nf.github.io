title: next-nf
description: >-
  Open-source 3GPP mobile-core network functions — SMF/PGW, HSS/UDR/UDM,
  PCF/PCRF, CHF/OCS/OFCS — built in Erlang/OTP.
url: https://next-nf.github.io
author: Nathan Foster
lang: en_US
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
# Google Search Console verification (jekyll-seo-tag emits the <meta> tag).
google_site_verification: "w4Hp1g9x8iuYBH3nB6dCHThrgUgQZ0Su6O-rM-oJEOk"

# Repo-only files that must NOT be published to the built site.
# (Jekyll still keeps its own defaults — Gemfile/Gemfile.lock — excluded too.)
exclude:
  - CLAUDE.md
